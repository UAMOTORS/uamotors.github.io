---
import { getCollection } from "astro:content";
import MainLayout from "../layouts/MainLayout.astro";
import siteConfig from "../site.config";
import { sortItemsByDateDesc, withBase } from "../utils/helpers";
import PostCardPreview from "../components/PostCardPreview.astro";
import Button from "../components/Button.astro";
import { Image } from "astro:assets";
import heroImage from "../assets/images/pixeltrue-space-discovery.svg";
import ArrowRight from "../icons/ArrowRight.astro";

const allPosts = (await getCollection("blogs")).sort(sortItemsByDateDesc);
---

<MainLayout
  activePage="home"
  pageTitle={"UAMOTORS"}
>
  <div
    class="jumbo-banner flex flex-col sm:flex-row sm:items-center sm:justify-between w-full px-4 my-12 gap-6"
  >
    <div class="w-full sm:w-fit sm:max-w-1/2">
      {
        siteConfig.hero?.eyebrowText && (
          <span class="tag rounded-full text-xs px-2 py-0 border border-gray-500 kanit-light">
            {siteConfig.hero?.eyebrowText}
          </span>
        )
      }
      <h1 class="sigmar-ff-italic leading-tight text-balance sm:pt-0!">
        {siteConfig.hero?.title}
      </h1>
      <p class="kanit-regular text-balance text-xs sm:text-base">
        {siteConfig.hero?.text}
      </p>
      <div
        class="flex flex-wrap items-center text-xs sm:text-base gap-x-2 gap-y-8 mt-3 sm:mt-12"
      >
        {
          siteConfig.hero?.actions?.map((action, index) => (
            <Button
              href={action.href}
              class={index === 0 ? "space-btn-filled" : "space-btn-outlined"}
            >
              {action.text}
            </Button>
          ))
        }
      </div>
    </div>
    <div class="w-full sm:w-fit sm:max-w-1/2">
      <!-- {siteConfig.hero?.image && <Image src={siteConfig.hero?.image?.src} alt={siteConfig.hero?.image?.alt || ""} inferSize={true} />} -->
      <Image
        class="cover-img"
        src={heroImage}
        alt={"Astronaut in space"}
        width={400}
        height={300}
        transition:name="hero-image"
      />
    </div>
  </div>
  <div class="page-content">
    <div id="instagram-feed" class="flex flex-col items-center justify-center w-full">
      <h2 class="sigmar-ff text-balance pt-0! pb-8! text-space-red dark:text-space-yellow">Síguenos en Instagram</h2>
      
      <div class="w-full max-w-4xl mx-auto mt-4 overflow-hidden">
        <!-- Place <div> tag where you want the feed to appear -->
        <div id="curator-feed-default-feed-layout"><a href="https://curator.io" target="_blank" class="crt-logo crt-tag">Powered by Curator.io</a></div>

        <!-- The Javascript can be moved to the end of the html page before the </body> tag -->
        <script type="text/javascript" is:inline>
        /* curator-feed-default-feed-layout */
        (function(){
        var i,e,d=document,s="script";i=d.createElement("script");i.async=1;i.charset="UTF-8";
        i.src="https://cdn.curator.io/published/a1e46c5e-ad11-49f1-b60c-867fd268c002.js";
        e=d.getElementsByTagName(s)[0];e.parentNode.insertBefore(i, e);
        })();
        </script>
      </div>

      <Button href="https://www.instagram.com/uamotors" target="_blank" class="space-btn-filled mt-8">
        Ver perfil completo <ArrowRight class="w-5 h-5 fill-current ml-4" />
      </Button>
    </div>
  </div>
</MainLayout>

<style is:global>
   /* Forzar color oscuro en texto de Curator cuando el sitio está en modo oscuro */
   html.dark #curator-feed-default-feed-layout .crt-post-header span,
   html.dark #curator-feed-default-feed-layout .crt-post-header a,
   html.dark #curator-feed-default-feed-layout .crt-post-header div,
   html.dark #curator-feed-default-feed-layout .crt-date,
   html.dark #curator-feed-default-feed-layout .crt-post-date,
   html.dark #curator-feed-default-feed-layout .crt-time {
    color: #111827 !important;
  }

  /* Neutralizar efectos de hover (cambio a rojo/amarillo de la página o efectos nativos de Curator) en CUALQUIER enlace del widget */
  #curator-feed-default-feed-layout a:hover,
  #curator-feed-default-feed-layout .crt-date:hover,
  #curator-feed-default-feed-layout .crt-post-date:hover,
  #curator-feed-default-feed-layout .crt-time:hover {
    color: inherit !important;
    opacity: 1 !important;
    text-decoration: none !important;
  }
</style>
